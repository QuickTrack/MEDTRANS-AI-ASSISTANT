Placeholder for application assets.

Add the following files for a complete production build:
  - assets/icon.ico        (application + executable icon, 256x256 multi-res)
  - assets/logo.png        (hospital/brand logo used in exported documents)

The icon should be referenced by installer/medtrans.spec (`icon=`).
The logo path is configured at runtime via Settings → Logo.
